import React, { Component } from 'react'
import Search from '../search/search'

//instantiates the component for the search page
class SearchRoute extends Component {
    render() {
    return (
      <section>
        < Search />
      </section>
    );
  }
}

export default SearchRoute